# own_manager / Network sending — установка, настройка, как это работает

Заменяет ChituManager в связке CHITUBOX → печать: файл, нарезанный в
CHITUBOX и отправленный через «Отправка по сети», улетает напрямую на 
ScaleX LAN Manager.

## Установка

### Требования

- Windows (используются нативные Windows API — `QSharedMemory`-хук,
  `user32`/`kernel32` через ctypes; на другой ОС работать не будет).
- Python 3.10+ (проверено на 3.14). Любой установленный интерпретатор —
  через сам Python.org, через `py`/PyManager или Microsoft Store, не
  принципиально.
- Установленный Edge или Chrome (для окна пикера в режиме `--app` — см.
  ниже). Если ни того ни другого нет — own_manager откатится на открытие
  обычной вкладки браузера по умолчанию, работать продолжит, просто
  выглядеть будет как обычная страница, а не отдельное окно.
- CHITUBOX (Pro или обычный) — сам слайсер, уже настроенный на вашу ферму.
- Работающий ScaleX LAN Manager, доступный по сети с этой машины.

### Шаги

1. Скопировать папку `own_manager_package` куда угодно на диск (в этом
   README путь не важен — скрипт не привязан к своему расположению).

2. Поставить Python-зависимости:

   ```bash
   pip install pycryptodome
   ```

   Расшифровка блока настроек CTB-файла для точного определения принтера,
   под который был нарезан файл (см. раздел про точный парсер ниже). Не
   критично: без него own_manager сам переключится на резервный текстовый
   поиск по файлу — работать продолжит, просто чуть менее надёжно на
   редких форматах файлов.

3. Открыть [`own_manager.py`](own_manager.py) и поправить под свою ферму
   константы в начале файла (поиск по `SCALEX_HOST`):

   ```python
   SCALEX_HOST = "192.168.0.118"   # IP или хост вашего ScaleX LAN Manager
   SCALEX_PORT = 8082              # его порт
   ```

   Больше ничего вручную настраивать не нужно — список принтеров,
   рекомендованные параметры экспозиции и статус «подготовлен» own_manager
   всегда берёт живьём из `GET /api/printers` на этом хосте.

4. Первый запуск — из консоли, чтобы сразу видеть лог живьём:

   ```bash
   python own_manager.py
   ```

   Ожидаемый вывод (и то же самое в `C:\own_manager\own_manager.log`):

   ```
   === own_manager started PID=... ===
   === ScaleX: http://<ваш хост>:<порт> ===
   === HTTP server listening on 127.0.0.1:8917 ===
   === slicer_file_watcher: watching ...\SlicerFile ===
   === CHITUBOX protocol listening on 127.0.0.1:<случайный порт> ===
   CreateFileMappingW(...) OK (alreadyExisted=no), wrote "<порт>"
   === shared memory created=yes ===
   ```

   Если вместо `shared memory created=yes` видно `NO` — CHITUBOX уже
   держит этот сегмент занятым (сам открыт и уже подключился к
   ChituManager, либо ChituManager уже был запущен раньше). Закрыть и
   CHITUBOX, и ChituManager, снова запустить own_manager, потом уже
   открывать CHITUBOX.

5. Проверка: открыть CHITUBOX, нарезать что угодно, нажать «Отправка по
   сети». ChituManager запускаться не должен вообще — вместо него должно
   само собой открыться отдельное окно «Network sending» с выбором
   принтеров. Если открылся именно ChituManager — см. «Диагностика» ниже.

### Автозапуск (по желанию)

Чтобы own_manager поднимался сам при каждом входе в Windows, без
открытой консоли. В этой папке [`own_manager_autostart.vbs`](own_manager_autostart.vbs)
уже создан и настроен — если переносите own_manager на новую машину/в
новую папку, поправьте в нём путь (`WshShell.CurrentDirectory`) и
повторите шаг 2. С нуля (например, на новой машине) шаги такие:

1. Создать `own_manager_autostart.vbs` рядом с `own_manager.py`:

   ```vbs
   Set WshShell = CreateObject("WScript.Shell")
   WshShell.CurrentDirectory = "<путь до папки с own_manager.py>"
   WshShell.Run "pythonw.exe own_manager.py", 0, False
   ```

2. Положить ярлык на этот `.vbs`-файл в папку автозагрузки Windows:

   ```
   %APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\
   ```

   (Быстрее всего — набрать `shell:startup` в адресной строке
   проводника, она откроет эту папку напрямую.)

Готово — при следующем логине own_manager поднимется сам, тихо, в фоне
(`pythonw.exe`, без консольного окна).

Чтобы отключить — удалить ярлык из папки Startup, сам `.vbs` можно не
трогать.

Чтобы перезапустить вручную после правок `own_manager.py` (например,
поменяли `SCALEX_HOST`): закрыть все процессы `python.exe`/`pythonw.exe`,
запустившие `own_manager.py`, и запустить заново — двойным кликом по
`own_manager_autostart.vbs`, либо `python own_manager.py` из консоли,
если нужно видеть лог живьём. **Если открыты окна пикера с ещё не
отправленными файлами** — перезапуск их закроет, лучше сперва
отправить/закрыть их самостоятельно.

### Диагностика

- **Пикер не открывается, ChituManager запускается как раньше** — значит
  CHITUBOX не увидел own_manager. Проверить: own_manager вообще запущен
  (лог показывает `shared memory created=yes`)? CHITUBOX был открыт уже
  после запуска own_manager (сегмент разделяемой памяти читается один раз
  при старте CHITUBOX)? Если сначала открыли CHITUBOX, а own_manager
  запустили позже — закрыть CHITUBOX и открыть заново.
- **Порт 8917 занят** — own_manager сам откатится на случайный порт
  (сообщение в логе), пикер продолжит открываться нормально, просто адрес
  `http://127.0.0.1:8917/` для ручного захода будет неактуален в эту
  сессию.
- **Окно пикера не выходит на передний план** — проверено вживую, что
  работает (см. `_bring_window_to_front` в коде); если всё же не
  срабатывает, окно всё равно открывается — ищите его на панели задач.
- **Реальный принтер начал печать без включённой галочки «Запустить
  печать»** — не должно происходить (см. SESSION NOTES в самом
  `own_manager.py` про отказ от `bulk-uploads/from-draft`), но если
  увидите это — немедленно сообщите: это будет означать, что ScaleX
  сам поменял поведение своего API.

## Как это работает

### 1. CHITUBOX подключается к нам, а не к ChituManager

CHITUBOX узнаёт порт «менеджера» через именованный сегмент разделяемой
памяти Windows (`QSharedMemory` под капотом) с именем:

```
qipc_sharedmemory_ServerPort07a1a6dcfadd97d64f9f7f13063e6345d8b33ce8
```

own_manager создаёт этот сегмент сам (`CreateFileMappingW`/`MapViewOfFile`
через ctypes, см. `create_shared_memory()`) и пишет туда номер своего TCP
порта ASCII-строкой. CHITUBOX при клике «Отправка по сети» находит этот
порт и коннектится напрямую к own_manager — ChituManager.exe не
запускается вообще, ни разу.

### 2. Прямой триггер сохранения файла — без UI ChituManager

Сам CHITUBOX (не ChituManager) умеет обработать сообщение

```json
{"MsgType": "SaveFile", "FilePath": "<путь>"}
```

— это обрабатывает его собственная функция `ChituManager::saveSliceFile`
(найдено декомпиляцией `CHITUBOX Pro.exe` в Ghidra), которая либо
копирует уже нарезанный файл, либо реально режет модель, а потом пишет
результат по указанному пути. own_manager (`handle_client()`) ловит пинг
CHITUBOX `LoadWindow` с `"Visible": true` (шлётся при каждом клике
«Отправка по сети») и в ответ сразу просит `SaveFile` с путём во
временную папку `C:\own_manager\pending\`. Когда CHITUBOX присылает
подтверждение с `SavePath`, файл копируется в `C:\own_manager\received\`
и открывается окно выбора принтеров.

Резервный вариант на случай сбоя протокола — `slicer_file_watcher()`,
фоновый поток, который следит за папкой, куда ChituManager обычно
сохраняет нарезанные файлы (`%LOCALAPPDATA%\ChituManager\...\SlicerFile`),
и подхватывает файл оттуда, если он всё же появился таким путём.

### 3. Определение, под какой принтер нарезан файл

CTB-файл (формат «Chitubox CTB (Encrypted)», magic `0x12FD0107`) несёт
внутри зашифрованный блок настроек (AES-256-CBC, ключ/IV зашиты в сам
формат — это обфускация формата, не защита конкретного файла), а в нём —
поля `MachineNameOffset`/`MachineNameSize`, указывающие на открытый
ASCII-текст с именем целевой машины (`extract_ctb_machine_name()`).
own_manager читает его точно по этим полям (нужен `pycryptodome`); если
файл не подходит под эту структуру (другой формат/версия) — резервный
план: текстовый поиск копирайт-строки CHITUBOX, имя машины лежит прямо
перед ней. В окне пикера принтеры с несовпадающей моделью можно спрятать
галочкой «Подходит под файл» (включена по умолчанию, если машину вообще
удалось определить).

### 4. Пикер — окно приложения, оформленное под сам ScaleX

own_manager поднимает собственный HTTP-сервер (`127.0.0.1:8917`,
фиксированный порт) и на каждый пойманный файл открывает окно выбора
принтеров как отдельный процесс Edge/Chrome в режиме `--app`
(`open_app_window()`) — без адресной строки, само выходит на передний
план поверх CHITUBOX. Страница переиспользует настоящий `styles.css` со
ScaleX и его CSS-классы, поэтому выглядит частью того же приложения, а не
сторонним диалогом.

Раньше это было нативное окно через **pywebview** (на движке WebView2) —
без общего значка браузера в заголовке, немного аккуратнее визуально. Но
под реальной нагрузкой (большие файлы, длинные отправки) own_manager с
pywebview несколько раз то намертво зависал целиком (включая HTTP-сервер),
то беззвучно падал без единой строчки в логе или трассировки — то есть
ронял вообще всё, не только окно. Отдельный процесс браузера так не может:
максимум, что он утащит с собой при падении — самого себя, а сам
own_manager продолжит работать и подхватывать новые файлы от CHITUBOX.
Понятную причину падений pywebview найти не успели (см. «Заброшенные
подходы» ниже) — надёжность оказалась важнее внешнего вида, откатились.

Возможности страницы:
- Переименование файла перед отправкой.
- Поиск принтера, чекбоксы «Показывать включённые» / «Скрывать занятые» /
  «Подходит под файл».
- Мультивыбор принтеров, «выбрать все видимые» / «снять всё».
- Список принтеров обновляется живьём (раз в 5с) пока окно открыто и
  отправка ещё не началась — статус «подготовлен», занят/свободен и т.д.
  не устаревают, пока вы выбираете, куда слать.
- На каждой карточке принтера — если у него в ScaleX заданы рекомендованные
  параметры экспозиции, точечная галочка «применить рекомендации»
  индивидуально для этого принтера (по умолчанию выключена).
- Бейдж «Принтер подготовлен» / «Не подтверждён» — то же самое, что
  показывает сам ScaleX (`operatorPrepared`), чисто информационно.
- Галочка «Запустить печать сразу после отправки».
- Прогресс-бар отправки — свой, под каждой выбранной карточкой принтера
  (не общий на весь экран), с реальными фазами и процентом.
- Кнопка «Закрыть окно» появляется, когда отправка завершена (успешно
  или с ошибкой).

### 5. Отправка в ScaleX

Всегда по одному запросу на принтер, но **параллельно** (по потоку на
принтер, не дожидаясь друг друга) — через одиночные эндпоинты
`POST /api/printers/{id}/files` (быстрый путь, без изменений таймингов)
или `POST /api/ctb/patch-and-upload` (когда для принтера реально нужно
применить рекомендованную экспозицию). Именно на этих эндпоинтах
подтверждённо работает `X-Start-Print`/`autoStart` — «поставить в очередь
без печати» либо «поставить и сразу напечатать», в зависимости от
галочки. Bulk/`from-draft` эндпоинты ScaleX **намеренно не используются**
(см. SESSION NOTES в `own_manager.py`) — они помечены как
«экспериментальные» в самом ответе ScaleX и однажды запустили печать на
живом принтере, даже не получив в запросе ни одного поля про автостарт.

`/api/ctb/patch-and-upload` сам умеет патчить экспозицию двумя способами
(`patchMode` в теле запроса, найдено в её собственном `app.js` — точно тот
же тумблер, что в её одиночной отправке): `"fast"` — точечная правка полей
заголовка CTB, быстро; `"full"` — полная пересборка файла через UVTools
(это тот режим, что реально занимает время, у самого ScaleX в интерфейсе
на него отдельное предупреждение). own_manager технически умеет попросить
любой из двух (`patch_and_upload_single(..., patch_mode=...)`), но **в
пикере переключатель не показывается — всегда шлётся `"fast"`**. При
живом тесте `"full"` на этой ферме own_manager (не сам ScaleX — прямой
запрос к ScaleX в обход own_manager отработал нормально, за ~15 секунд)
один раз завис намертво вместе со всем процессом, включая открытое окно
пикера, воспроизвести стабильно не успели. Причина не найдена; пока не
разберёмся — трогать это не стоит.

## Файлы в папке

- `own_manager.py` — рабочий файл, всё описанное выше.
- `own_manager_autostart.vbs` — тихий запуск для автозагрузки (см.
  «Автозапуск» выше — создаётся один раз при установке).
- `HOWTO_bypass_chitumanager.md`, `HOWTO_connect.md`, `findings.txt` —
  черновые заметки из более ранней стадии реверса (discovery-механизм,
  первые гипотезы) — историческая справка, актуальная схема описана здесь.
- `own_manager.c`, `own_manager.exe` — самый первый прототип на чистом C
  (без Python), только discovery + голый TCP-эхо, до находки прямого
  триггера `SaveFile`. Не используется, оставлен для истории.
- `chitubox_login.py` — отдельный скрипт логина в облако CHITUBOX
  (`getpass`, не хранит пароль). Не используется текущей архитектурой.

## Заброшенные подходы (чтобы не наступать на те же грабли)

1. **Полная замена ChituManager** (свой клон Angular-интерфейса ChituManager
   + подмена XHR/fetch под чужой логин). Дошли до реального обхода логина
   и рендеринга всей фермы из 48 принтеров, но финальный триггер —
   WebSocket-коннект карточки принтера — не срабатывал ни при каком
   воспроизведении, несмотря на точное соответствие декомпилированному
   коду. Брошено.
2. **Дать CHITUBOX открыть настоящий ChituManager и смотреть его окно
   через UI Automation** (pywinauto) — отклонено как избыточное решение.
3. **SSLKEYLOGFILE для расшифровки трафика ChituManager** — заблокировано
   средой выполнения.
4. **pywebview вместо Edge `--app` для окна пикера** — давало более
   аккуратный титлбар (без общего значка браузера), но за время реального
   использования own_manager с pywebview минимум трижды падал целиком
   (не только окно — весь процесс, включая HTTP-сервер, переставал
   отвечать CHITUBOX):
   - `win.events.closed` намертво вешал процесс в момент регистрации
     обработчика сразу после `create_window()` — воспроизведено стабильно,
     100% случаев;
   - при `patchMode: "full"` (полная пересборка CTB через UVTools,
     долгий сетевой запрос к ScaleX в фоновом потоке) один раз завис весь
     процесс целиком, включая открытое окно;
   - при обычной отправке большого файла (200-300+ МБ) as минимум дважды
     процесс тихо падал вообще без следа — ни исключения в логе, ни
     трассировки, ничего.
   Изолированные тесты (реальное окно + фоновая долгая сетевая операция,
   на локальном тестовом сервере) зависание не воспроизвели — то есть
   баг либо специфичен для реальной связки с ScaleX, либо просто
   недетерминирован. Точную причину найти не успели. Заменили на
   Edge/Chrome `--app` (отдельный OS-процесс на каждое окно) — эта схема
   уже отработала весь сеанс до pywebview без единого зависания, и в
   худшем случае падает только сам браузерный процесс, а не own_manager
   целиком. Уникальность заголовка окна для `FindWindowW` (раньше через
   `win.events`) теперь просто невидимые символы в `document.title`,
   передаваемые через `&wtag=N` в URL (см. `open_app_window()`).

Текущая схема (прямой TCP-триггер `SaveFile`) обходит первые три
проблемы — ChituManager вообще не участвует, а значит и его
UI/логин/WebSocket не нужны.
